#!/bin/bash

date #prints the current day of the week, date (day, month, year), time (hours, minutes, seconds, AM/PM, timezone) 
cal #prints a nice little calander
pwd #prints path of current directory
ls #prints all files in current directory
